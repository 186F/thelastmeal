# Milestone 2 — Phase 3 Unattended Orchestrator — Implementation Report

**Package:** `1.8.0`
**Governing documents:** [`MILESTONE_002_SPARSE_COGNITION_AUTOMATION_IMPLEMENTATION_BRIEF.md`](MILESTONE_002_SPARSE_COGNITION_AUTOMATION_IMPLEMENTATION_BRIEF.md) §19–§20, and [`MILESTONE_002_SCOPE_RULING_AND_AMENDMENTS.md`](MILESTONE_002_SCOPE_RULING_AND_AMENDMENTS.md) Phase 3 ("Build R1 first against existing deterministic and per-decision paths").
**Scope discipline:** no M2 action prompt or per-decision condition (Phase 4); no policy patches, compiler, interpreter, or novelty broker (Phase 5); no live API calls anywhere in Phase 3 — every model-backed path is exercised keylessly through the fake-adapter gateway. No canonical simulation-mechanics change; all fourteen golden hashes byte-identical.

## 0. Audit remediation record (July 30, 2026)

[`MILESTONE_002_PHASE3_ORCHESTRATOR_AUDIT.md`](MILESTONE_002_PHASE3_ORCHESTRATOR_AUDIT.md) requested changes on nine blockers plus mediums. All are remediated in this head; the accepted architecture (§3 of the audit) is unchanged.

| Audit finding | Resolution |
| --- | --- |
| 1 — release metadata | `package-lock.json` synchronized to 1.8.0; a test fails on any future package/lock version disagreement |
| 2 — treatment identity unbound | Plans carry a reviewed `expectedTreatment` (model, serving route, allowFallbacks=false, requireParameters=true, prompt, condition, experiment) — required for any model attempt; the gateway's public config now exposes the nonsecret `servingProviderId` and the orchestrator verifies EVERY field before Start (typed `treatment-mismatch` refusal), records verified values in execution verdicts and the acknowledgement, binds them (with the study identity) into resume identity, and re-verifies the final manifest after finalization. Evidentiary plans must bind a validated Phase 2 study declaration (id/version/sha256/fingerprint checked against `study:validate`) |
| 3 — freeze unenforced | Evidentiary plans require a pinned SHA, a clean tracked worktree, and an output root OUTSIDE the repository; the exact plan bytes are archived into the sequence root and sha-verified; HEAD, package version, and the archived plan are re-checked before AND after every execution — drift during an attempt preserves the work as a `freeze-violation` |
| 4 — resume mutates/trusts | All identity checks run before any write (refused invocations are byte-read-only); the cost acknowledgement is create-once immutable; every completed execution is SEALED (per-file sha256 + aggregate) and seals are revalidated on resume — missing/altered/added evidence is a typed refusal; a nonempty stateless root is refused; a completed-sequence resume is a NO-OP that verifies seals and launches nothing; `evaluateFromState` fails on a missing fingerprint |
| 5 — completion before sealing | Sequence lifecycle `in-progress → attempts-complete → validating → packaging → completed/failed` with typed `sequenceFailureReason`; `completed` is written only after the archive AND its receipt verify; secret-scan or packaging failure → `failed` |
| 6 — diagnostics incomplete | Every attempt (success or failure) saves `attempt-trace.zip`, final screenshot, final DOM, `console-log.json`, and a `diagnostics-manifest.json` (capture failures recorded explicitly, never silently); failed attempts additionally get failure-* artifacts, the thrown stage/message, and bounded Vite/process-log tails — so finalizer-stage failures have complete browser forensics |
| 7 — packaging not immutable | Writer lock moved OUTSIDE the packaged root; fresh archive in a temp path with atomic move (never an in-place ZIP update); portable forward-slash entries (bsdtar on Windows); extraction verification against the inventory (set + byte equality — stale entries cannot survive); sibling `.sha256` + `.receipt.json` receipts; nested archives (Playwright traces) extracted and secret-scanned; `m2:package` requires a completed, seal-valid sequence; CI uploads the complete evidence zip + receipt + failure drill with `if-no-files-found: error` |
| 8 — profiles/verdicts | Evidentiary plans must use EXACTLY the formal profile (heartbeat ≤60 s, stall 120 s, 75-min normal / 90-min gateway-stop timeouts, per-attempt-kind); a planned gateway stop must FIRE and is evidenced (observed tick, wall clock, accepted responses at stop, child exit) — an unfired stop fails as `gateway-stop-not-fired`; unplanned gateway death in a normal attempt fails as `gateway-died-unexpectedly`; replacements are restricted to the plan's registered `retryableFailureClasses` — integrity/treatment failures halt without spending another run |
| 9 — secret process boundary | Allowlisted child environments: Vite, batch, and fake-gateway children are built from a platform allowlist and structurally cannot carry `OPENAI_API_KEY`/`OPENROUTER_API_KEY`; fake-mode `loadGatewayConfig` no longer reads `.env.gateway` at all and blanks credential/model/route fields even against a poisoned process environment (tested with canary files and env); only the live gateway child receives the full environment (§19.15) |

Mediums: process provenance is an append-only `process-log.jsonl`; the lock uses a unique holder token outside the root; the rehearsal's gateway-stop counts are documented as the pre-registered INVARIANT (`emitted > attempted`, `attempted = completed`) rather than a timing-sensitive constant; `m2:pilot` correctly cites brief §31 **Phase 7** (rehearsal + pilot, after the Phase 6 adversarial audit).

## 0.2 Re-audit remediation record (July 30, 2026)

[`MILESTONE_002_PHASE3_ORCHESTRATOR_REAUDIT.md`](MILESTONE_002_PHASE3_ORCHESTRATOR_REAUDIT.md) accepted the §4 remediations and requested changes on nine remaining formal-evidence problems. All are remediated in this head; the accepted architecture is unchanged.

| Re-audit finding | Resolution |
| --- | --- |
| 1 (Critical) — archive/state divergence | Mutable operational state moved to a CONTROL ROOT beside the evidence root (`<root>.control/`: `sequence-state.json`, writer lock, packaging-helper log) — no inventoried evidence file changes after the archive is created. An immutable **`sequence-manifest.json`** (final attempt set, terminal verdicts, artifact/study statuses, dispositions, threshold verdicts, seal aggregates, gate results, freeze checkpoints) and the final report are written into the evidence root BEFORE the inventory; the report and manifest carry final outcome facts, never an in-flight lifecycle value. The sibling receipt is the authoritative post-archive seal. A completed no-op resume now verifies terminal seals, root-vs-inventory byte equality, the manifest, SEMANTIC revalidation, the archive hash, the `.sha256` sidecar, every receipt identity field, and a full extraction against the inventory — all read-only, all typed refusals. `m2:evaluate` on a completed root writes only to `<root>.derived/evaluation-<stamp>/`; `m2:package` verifies the recorded inventory (refusing a mutated root) and produces the NEXT versioned archive, pointing state at it while prior archives survive |
| 2 (Critical) — study not reconciled | New `studyReconciliation.ts` (versioned mapping `m2-study-plan-mapping-1.0.0`): before any write, `study.repositorySha/packageVersion/experiment/model/provider/promptVersions/conditionIds/scenarioIds/seeds (scenario-derived)/sampleSizeN (= planned primary attempts)/runOrderMethod (= 'plan-order')/liveCallBudget (plan ≤ study)/executionSettings (speed + caps vs effective attempts)/replacementPolicy/outputRoot (normalized)/metricVersions + analysisScriptVersion (vs installed)/thresholds (must bind the plan's attempt profile id+version)` are all compared; any disagreement is a typed refusal listing every mismatch. The exact study bytes are archived as `study.archived.json` with `study.freeze.archived.json`, and the archived bytes, the freeze record, AND the external registered file join every freeze check |
| 3 (Critical) — artifact vs study validity | New versioned attempt profiles (`attemptProfile.ts`, `m2-attempt-profile-1.0.0`): artifact gates, gateway lifecycle, generic treatment thresholds (`upstream-completion-ratio-bp`, `upstream-calls-attempted`), the taxonomy partition, a replacement-limit cap, and a stop rule — bound into the plan, the 16-field resume identity, and the study's thresholds record. Executions record `artifactStatus` / `studyStatus` / `thresholdVerdicts` separately: a strict-finalized run below the registered thresholds is preserved as **`invalid-treatment`** (a hard-stop class), never completed primary evidence and never mislabeled artifact corruption. `retryableFailureClasses` is now a CLOSED enum over the taxonomy's retryable-eligible partition — hard-stop classes are structurally unregistrable. Every terminal failure records a `replacementDisposition` (`permitted`/`forbidden`/`exhausted`); resume inspects the RECORDED disposition, so a non-retryable failure can never be revived after a restart. Phase 3 registers only the non-formal rehearsal profile, so every evidentiary/live plan is hard-refused (`formal-attempt-profile-required`) until Phase 4 registers the formal profile with its study |
| 4 (High) — freeze ends early | `checkFreeze` extended (lockfile version agreement, archived-plan config-fingerprint recomputation, attempt-profile registry agreement, archived study + freeze record + external study file) and now runs before/after every execution AND through the complete post-sequence pipeline: pre/post batch, pre/post evaluation + reporting, pre-package, and post-package BEFORE the receipt (the packaging `beforeReceipt` hook). Passed checkpoints are recorded in state and the manifest; drift anywhere fails the sequence as a preserved `freeze-violation` that is never packaged under the old SHA |
| 5 (High) — terminal evidence partially sealed | EVERY terminal execution is sealed — completed, failed, and interrupted (sealed as they survived, empty-set seal if the directory never existed) — with a status-tagged seal verified for ALL terminal executions on every resume. Completed executions are additionally SEMANTICALLY revalidated on resume (deterministic ledger validation, strict-finalized evidence load with the exact trace-to-ledger join, fingerprint recomputation compared byte-for-byte, final-manifest treatment cross-check). The sha256 inventory + recorded aggregate acts as the sequence-level seal over every non-attempt evidence file; a completed root is immutable raw evidence |
| 6 (High) — helper processes see the key | One `helperEnv` builder now covers EVERY non-live-gateway child: keep-awake (powershell/caffeinate/systemd-inhibit), zip/unzip/tar, git `execFileSync`, and Windows `taskkill`. Only `liveGatewayEnv` passes credentials. Keep-awake is LIVE-ONLY, acquired after the acknowledgement gate (a refused live launch never spawns it) with the release scope opening immediately. Boundary proven by spawning real children that PRINT what they observe under a poisoned parent environment |
| 7 (High) — non-atomic archive fallback | Archives are staged as a dot-tmp SIBLING in the destination directory (same volume — the commit is a true `rename`, the copy fallback is deleted), fsynced, verified by extraction BEFORE commit, renamed to a versioned immutable destination (`<sequenceId>-evidence-NNN.zip`) that must not already exist — a prior archive is never deleted or overwritten — and the receipt is written ONLY after the final path hashes correctly and the post-package freeze check passes |
| §12 (Medium-high) — untyped finalization failures | Closed failure taxonomy (`failureTaxonomy.ts`, `m2-failure-taxonomy-1.0.0`): the generic `error` class is gone. Post-browser stages are typed (`deterministic-validation-failed`, `prepare-run-failed`, `strict-finalize-failed`, `evidence-enrichment-failed`, `fingerprint-failed`, `final-treatment-verification-failed`, `execution-seal-failed` — compile-time-checked against the taxonomy), the stage implementations are injectable, and failure-injection drills cover every stage plus diagnostic-capture failure, proving browser forensics survive and the exact stage lands in the structured `failure-manifest.json` |
| §13 mediums | 13.1: `ProcessManager` counts record-write and stop failures and reconciles children; the report carries process-provenance health and an evidentiary sequence FAILS as `process-evidence-incomplete` when provenance is incomplete. 13.2: Playwright version, browser version, headless mode, and context/tracing options are recorded per execution before sealing. 13.3: a load-generation token + main-frame navigation counter fail an attempt as `unexpected-navigation` on any reload; navigation counts are recorded per execution. 13.4: free-disk preflight against a configurable per-plan `evidenceSizeBudgetBytes` (default 8 GiB), root-size checks before and after every execution (`evidence-budget-exceeded`), an archive-size cap, time-bounded nested scans, and tracing without source embedding; chunked tracing is explicitly scheduled before live Stage A. 13.5: the PR body and this report now state the artifact contract accurately (see §3) |

## 0.3 Focused re-audit remediation record (July 31, 2026)

[`MILESTONE_002_PHASE3_ORCHESTRATOR_FOCUSED_REAUDIT.md`](MILESTONE_002_PHASE3_ORCHESTRATOR_FOCUSED_REAUDIT.md) accepted the second remediation's architecture and requested changes on four focused issues. All four are remediated in this head; the accepted architecture is unchanged.

| Focused finding | Resolution |
| --- | --- |
| 1 (High) — signal exits mistaken for running processes | `ManagedProcess` now records explicit TERMINAL STATE: `hasExited()`, `terminalOutcome()` (the exact numeric code, `signal:<NAME>`, or `spawn-error`), `numericExitCode()`, `exitSignal()`, and `stopRequested()`. Liveness (`health().runningChildren`) is `hasExited()`, never the numeric exit code — a SIGTERM/SIGKILL-killed child (numeric code null) is terminal, so a signal-killed Vite can no longer fail a formal sequence as a phantom unreconciled child. Unexpected-gateway-death detection classifies terminal state via `classifyChildTerminalState` (still-running / planned-stop-completed / orchestrator-stop-completed / unexpected-terminal-exit — `stop()` marks orchestrator intent BEFORE acting), so an externally signal-killed gateway is `gateway-died-unexpectedly` while a planned or orchestrator-initiated stop never is — and the planned-stop TRIGGER itself (`issuePlannedGatewayStop`) refuses a gateway that already exited before the stop tick, so an earlier external death can never be absorbed as a planned stop. A spawn error is terminal too, its async `error` event absorbed so the typed refusal is the only failure signal. The process table and report retain the exact code or signal. Proven by REAL spawned children: exit 0, nonzero exit, SIGTERM, SIGKILL, spawn error, signal-killed-not-running, external-kill classification, planned-stop classification, and the dead-before-tick trigger refusal |
| 2 (High) — Chromium inherits the credential environment | New `browserChildEnv` (childEnv.ts): the Chromium OS process is launched with the REDUCED environment — platform base only, plus an explicit headed-mode allowlist (`DISPLAY`, `XAUTHORITY`, `WAYLAND_DISPLAY`, `DBUS_SESSION_BUS_ADDRESS`) — never a `process.env` spread. `chromium.launch({ env: browserChildEnv(process.env, options.headed) })` is asserted at source level, and the child-boundary suite enumerates Chromium explicitly: a spawned child under the poisoned parent environment observes neither `OPENROUTER_API_KEY` nor `OPENAI_API_KEY` in headless or headed mode, and headed mode restores ONLY the display/session variables |
| 3 (Critical) — late packaging failures strand completed datasets | New **packaging-ready recovery path**: once a valid inventory exists the evidence root is FINAL, and `--resume` routes to a packaging-ONLY path that (read-only, before any write) verifies terminal seals, semantic revalidation, exact root-vs-inventory byte equality, and the authoritative manifest against control state; launches NO Vite, Chromium, or gateway and never appends to the evidence-root process log; never reruns or rewrites evaluation, report, or manifest; handles prior material EXPLICITLY (crash-leftover staged `.tmp` siblings removed; committed-but-unreceipted archives preserved and recorded in the new `supersededArchives` control-state field); re-runs only the freeze verification (`pre-package:recovery`, `post-package:recovery` before the receipt) and the packaging transaction to the NEXT versioned archive; and writes `completed` only after the archive verifies and the receipt exists. The packaging transaction gained a versioned stage seam (`onStage`: zip/size/extraction/commit/sidecar/receipt; the post-package freeze check is `beforeReceipt`), and failure-injection drills at ALL SEVEN §6.4 points run through the real `orchestrateSequence` resume — proving state records the failure, evidence stays intact, recovery launches nothing and changes no inventoried byte, the next versioned archive + receipt appear, and a tampered root or diverged control state is refused with the sequence never completing. The rehearsal's new part 8 drives the full loop (real attempt → injected receipt-stage failure → recovery → verified no-op resume). `PACKAGING_VERSION` → 2.1.0, `SEQUENCE_STATE_VERSION` → 2.1.0 |
| 4 (Medium-high) — two advertised checks weaker than claimed | §7.1: the archived `study.freeze.archived.json` is now verified at EVERY freeze checkpoint like every other frozen artifact — parsed with each field compared against the frozen identity (`studyId`, `studyVersion`, `planSha256`, `configFingerprint`, each violation naming the field) AND its exact bytes hashed into `FreezeIdentity.studyFreezeRecordSha256` (a byte-level change that preserves valid JSON and field values still violates); the record content has one shared constructor (`studyFreezeRecordContent`) for writer and checker; tamper tests cover every field, byte-preserving reserialization, unparseable/non-object JSON, and deletion. §7.2: `reconcileManifestWithState` makes the IMMUTABLE manifest authoritative over control state on every completed verification (the no-op resume, the packaging recovery, AND standalone `m2:package`): all 17 identity fields, planned-attempt coverage, and the full execution history — count, order, IDs, terminal statuses, failure classes and stages, artifact/study statuses, replacement dispositions, threshold verdicts, run IDs, seal aggregates, navigation counts, browser provenance, verdicts, timestamps — must agree exactly, with the manifest's freeze checkpoints an exact PREFIX of the control state's (the pre/post-package and recovery checkpoints legitimately append after the manifest is written). A removed failed execution, an added execution, a reorder, or any edited field is a typed refusal listing every disagreement |

**Pre-push adversarial round (July 31, 2026).** Before requesting re-audit, the remediation was adversarially reviewed by four independent Opus 5 @ xhigh auditors (one per finding) with every claim independently refuted or confirmed by further Opus 5 @ xhigh verifiers (17 raw claims, 5 confirmed). All five confirmed defects are fixed in this head:

1. The planned gateway-stop trigger could absorb a gateway that ALREADY died before the stop tick as a "planned stop". `issuePlannedGatewayStop` now refuses when the gateway has exited — the attempt fails as `gateway-died-unexpectedly`, the stop is never marked issued, and the browser driver captures terminal diagnostics for hook-thrown typed failures (tested with real killed children).
2. The packaging-recovery inventory was self-certifying: a rewritten inventory matching a mutated root would have passed. `packageSequence` now reports the aggregate through `onInventory` and the orchestrator persists it in CONTROL state BEFORE the inventory file exists; recovery REQUIRES the recorded aggregate and refuses a disagreement or an unrecorded (planted) inventory — both drilled, including the rewritten-inventory-covering-a-tamper attack.
3. Six of the seven §6.4 failure-recording proofs were fixture-written rather than orchestrator-recorded. All seven drills now drive the FAILING leg through the real `orchestrateSequence` pipeline too, including the `freeze-violation:packaging` failure branch, before the recovery leg runs.
4. Standalone `m2:package` minted receipts from unreconciled control state. It now reconciles the immutable manifest against control state and derives the receipt identity from the MANIFEST — an edited control state can neither pass nor author a contradicting receipt (tested).
5. A study freeze record containing the JSON literal `null` (or an array/string) crashed `checkFreeze` with an untyped TypeError; it is now a typed `freeze-violation` (tested for `null`, `[]`, strings, and unparseable bytes).

## 0.4 Final targeted audit remediation record (July 31, 2026)

[`MILESTONE_002_PHASE3_ORCHESTRATOR_FINAL_TARGETED_AUDIT.md`](MILESTONE_002_PHASE3_ORCHESTRATOR_FINAL_TARGETED_AUDIT.md) accepted all prior remediation areas and requested changes on three final merge blockers. All three are remediated in this head; no accepted subsystem is redesigned.

| Final blocker | Resolution |
| --- | --- |
| 1 — post-spawn `error` treated as terminal | Terminal state is established ONLY by a genuine `exit` event. A post-spawn `error` event is a PROCESS-OPERATION DIAGNOSTIC — recorded per child (`processErrors()`), counted in health (`processErrorCount`, `lastProcessError`), written to the append-only process log as `process-error` — and the child stays in `runningChildren` until it actually exits, so an errored-but-alive Vite or gateway can never hide behind a false-green provenance gate. `stop()` now ends with a BOUNDED confirmation wait: graceful kill → grace → force kill → grace → if termination is still unconfirmed it returns `stop-unconfirmed`, counts a stop failure, and leaves the child unreconciled — provenance FAILS rather than reporting a shutdown that never happened. The no-PID spawn refusal is preserved; gateway-death classification consumes only genuine terminal state. The §3.4 matrix is proven with real children: the corrected post-spawn-error sequence (error while alive → not exited → still running → diagnostic visible → real kill → genuine terminal state → diagnostic still visible), failed-kill-then-real-exit, classification-only-after-genuine-termination, and an unkillable-child stop that reports `stop-unconfirmed` |
| 2 — inventory aggregate not recomputed | One shared strict validator (`validateInventory`) now authenticates every inventory before use: schema shape, well-formed lowercase SHA-256 for every entry and the aggregate, no duplicate paths, no absolute/traversal/dot-segment/backslash/non-normalized paths, canonical sorted order, and the aggregate RECOMPUTED from the exact validated entries with equality required — the aggregate field is cryptographically bound to the entry list. `readInventory` and `verifyTreeAgainstInventory` both call it, so completed resume, standalone `m2:package`, packaging recovery, and archive extraction verification all refuse the same malformed inventory consistently. Only after entry-level authentication is the aggregate compared against the control-recorded value and the tree against the entries. The §4.2 bypass (alter a file, update only its entry hash, keep the aggregate) is drilled on the recovery path (refused before any archive or state mutation) and on the completed paths (`verifyCompletedSequence` and `m2:package`), plus a 14-case malformed matrix and a valid-canonical control case |
| 3 — recovery verified before locking | `resumePackagingReady` now acquires the control-root single-writer lock FIRST (only immutable identity/path prechecks run before dispatch), then RE-READS `sequence-state.json` and the inventory under the lock and re-runs everything on those fresh objects: resume identity, resumability, terminal seals, semantic revalidation, aggregate authentication, tree-vs-inventory equality, and manifest reconciliation. If the sequence became `completed` while this invocation waited, it runs the complete no-op verification and returns without another package transaction; otherwise the transaction runs with the lock held through the receipt and the final control-state write, and no stale in-memory state object is ever written. The §5.4 race is drilled deterministically via an `onBeforeRecoveryLock` seam: a competing invocation completes recovery first → the delayed invocation re-reads under the lock, verifies whole, returns a no-op, creates no additional archive, and overwrites nothing; and a state mutation made while waiting for the lock is OBSERVED by the post-lock re-read and refused |

**Pre-push adversarial round (July 31, 2026, final blockers).** Before requesting targeted verification, this remediation was adversarially reviewed by three independent Opus 5 @ xhigh auditors (one per blocker) with every claim independently refuted or confirmed by further Opus 5 @ xhigh verifiers (14 raw claims, 1 confirmed). The confirmed gap — §5.4's required *inventory*-changed-while-waiting drill was missing, leaving the post-lock inventory re-read without a discriminating test — is fixed in this head: a seam-time inventory entry rewrite is now observed by the post-lock re-read and REJECTED before any recovery-state write, with the control state byte-unchanged and no archive created.

## 1. What Phase 3 delivers

| Component | Files | Purpose |
| --- | --- | --- |
| Automation contract | `src/shared/automationContract.ts` | the stable selector/data-field/text surface the driver uses (§19.7), pinned to the live DOM by an e2e contract test |
| Plan schema | `scripts/experiments/m2/planSchema.ts` | strict plan contract: condition/gateway cross-rules, §19.17 evidentiary 1× pacing, §19.14 worst-case budget refusal incl. replacements, byte-exact plan hash, attempt-profile binding, closed retryable-class enum |
| Failure taxonomy | `scripts/experiments/m2/failureTaxonomy.ts` | the closed, versioned failure vocabulary (retryable-eligible vs hard-stop), `AttemptFailure` with stage, replacement-disposition assessment |
| Attempt profiles | `scripts/experiments/m2/attemptProfile.ts` | versioned execution/threshold profiles separating artifact-valid from study-valid; generic treatment-threshold evaluation over enriched fingerprints |
| Study reconciliation | `scripts/experiments/m2/studyReconciliation.ts` | the versioned semantic mapping proving a plan IMPLEMENTS its registered study (repository/release/treatment/sample/budget/pacing/output/metric fields) |
| Sequence state | `scripts/experiments/m2/sequenceState.ts` | atomic control-root `sequence-state.json` (§19.11), exact 16-field resume-identity refusal, terminal seals for completed/failed/interrupted executions, recorded replacement dispositions gating resume |
| Sequence verification | `scripts/experiments/m2/sequenceVerification.ts` | resume-time SEMANTIC revalidation of completed evidence, exact manifest-vs-state reconciliation (the immutable manifest is authoritative), and the full completed-sequence verification (seals, inventory, manifest, archive, receipt, extraction) |
| Process manager | `scripts/experiments/m2/processManager.ts` | child spawn with signal-aware TERMINAL-state recording (exact code, signal, or spawn error), log capture, graceful-then-forced shutdown, and terminal-state classification for gateway-death detection (§19.5) |
| Gateway driver | `scripts/experiments/m2/gatewayDriver.ts` | fresh gateway child per model attempt on the fixed port; keyless fake mode pins EVERY setting via env so `.env.gateway` values cannot leak into rehearsals; live mode leaves credential loading entirely to the child (§19.15) |
| Vite driver | `scripts/experiments/m2/viteDriver.ts` | one strict-fixed-port server per sequence (5199), `VITE_MODEL_GATEWAY_URL` injected, occupied port = refusal (§19.6) |
| Browser driver | `scripts/experiments/m2/browserDriver.ts` | fresh Chromium context per attempt; real operator controls; semantic DOM polling; heartbeats (§19.9); stall watchdog + run timeout (§19.9–§19.10); downloads into the attempt directory; in-browser replay gate; failure diagnostics (screenshot, DOM, Playwright trace) |
| Run finalizer | `scripts/experiments/m2/runFinalizer.ts` | deterministic attempts: full ledger validation + bare fingerprint; model attempts: M1 `prepareRunDirectory` → strict `finalizeRunDirectory` → Phase 2 strict-finalized evidence layer → ENRICHED fingerprint — every stage TYPED and injectable for failure drills |
| Sequence evaluation | `scripts/experiments/m2/evaluateSequence.ts` | cross-run Phase 2 comparisons over completed executions; derived, regenerable; derived-output directory for completed (immutable) roots |
| Packaging | `scripts/experiments/m2/{secretScan,packageEvidence}.ts` | evidence secret scan (hard failure, time-bounded nested scans), SHA256 inventory (created once, verified thereafter), versioned immutable archives staged on the destination volume with an atomic rename commit, a receipt written last, a versioned stage seam for failure drills, and explicit stale-staged/unreceipted-archive handling for recovery (§19.4) |
| Keep-awake | `scripts/experiments/m2/keepAwake.ts` | live-only cross-platform sleep-inhibition lease under the nonsecret helper environment; live plans without a lease require `--allow-sleep-risk` (§19.13) |
| Orchestrator + CLI | `scripts/experiments/m2/{orchestrate,cli,reporting}.ts` | the §19.4 state machine end to end; `m2:orchestrate` (with `--resume`), `m2:evaluate`, `m2:package`, `m2:pilot` (Phase 7 refusal stub), `test:m2` |
| Self-verifying rehearsal | `scripts/experiments/m2/rehearse.ts`, `experiments/m2/plans/{rehearsal,failure-drill,packaging-drill}.json` | `m2:rehearse`: the keyless three-attempt sequence, the gateway-stop drill, idempotent resume, identity-refused resume, the preserved-failure drill, and the packaging-recovery drill — asserted, not just run |
| Operator runbook | `documentation/MILESTONE_002_CLAUDE_OPERATOR_RUNBOOK.md` | §20.2 supervision document |

## 2. The ten Phase 3 proofs (scope ruling)

| Property | Where proven |
| --- | --- |
| Process isolation | fresh gateway child + fresh browser context per attempt; PIDs/exit codes recorded; sequential attempts (`orchestrate.ts`, report process table) |
| Fresh gateway per run | `gatewayDriver.startGateway` per model attempt; per-attempt caps; process-wide budget resets by construction |
| Browser isolation | `browser.newContext()` per attempt; no cookies/storage/page reuse (`browserDriver.ts`) |
| Downloads | Playwright `download` events saved directly into the attempt directory under the app's own filenames |
| Replay | in-browser `Replay latest` must report `match` before the context closes; deterministic + model attempts alike |
| Finalization | strict `finalizeRunDirectory` (status `completed`) + Phase 2 evidence layer with the exact trace-to-ledger join |
| Failure preservation | stall watchdog, run timeout, and every TYPED stage preserve and SEAL the attempt directory + diagnostics + failure manifest; the failure drill asserts it, including the recorded forbidden disposition |
| Resume behavior | atomic control-root state, §19.11 identity refusal (16 fields), recorded-disposition gating, interrupted-execution preservation + sealing, semantic revalidation, and the fully verified no-op resume — all asserted by `m2:rehearse` |
| Secret scanning | `secretScan.ts` over the whole sequence tree incl. nested archives (time-bounded); packaging refuses on any finding; unit-tested patterns |
| Portable packaging | SHA256 inventory + versioned immutable ZIP with same-volume atomic commit, extraction verification, and receipt-last ordering (`packageEvidence.ts`) |

## 3. Rehearsal evidence (keyless)

`npm run m2:rehearse` (self-verifying; also a CI step) on the implementation head:

`npm run m2:rehearse` self-verifies, per run (fake-adapter counts vary with timing, so the rehearsal pre-registers INVARIANTS, not constants):

- All three attempts (deterministic baseline; per-decision fake gateway; per-decision fake with the planned tick-900 gateway stop) complete unattended with in-browser **replay matches**; both model attempts pass **pre-Start treatment verification** (`fake-adapter`/`local` against the gateway's public config), **strict-finalize `completed`**, produce Phase 2-enriched fingerprints, and every execution records **`artifact-valid` + `study-valid`** under the rehearsal attempt profile with passing threshold verdicts, per-execution browser provenance, exactly one main-frame navigation, and a **completed seal**. The root still matches its inventory byte-for-byte AFTER completion, the final evidence manifest describes the completed attempt set, the named freeze checkpoints all passed, and the VERSIONED archive (`…-evidence-001.zip`) verifies against its `.sha256` sidecar and receipt.
- The gateway-stop drill reproduces the Milestone 1 Run 6 shape keylessly — the enriched fingerprint must satisfy **`emitted > attempted` and `attempted = completed`**, and the stop is EVIDENCED (fired flag, observed tick, wall clock, accepted responses at stop, child exit) — satisfying §19.16's requirement that the gateway-stop path be demonstrated keylessly before any live spend.
- The completed-sequence resume is asserted to be a **no-op** that VERIFIES the evidence whole — terminal seals, root-vs-inventory byte equality, the manifest, semantic revalidation of every completed execution, the archive hash, sidecar, receipt fields, and a full extraction — while changing no byte and launching nothing; the mutated-plan resume is refused with `resume-identity-mismatch`.
- The failure drill preserves a **sealed** `run-timeout` attempt with full diagnostics, a typed `failure-manifest.json` (`run-timeout` at the `browser` stage), and a RECORDED `forbidden` replacement disposition — and a subsequent `--resume` of the failed drill is refused by that recorded disposition (`resume-blocked-forbidden-failure`), proving a restart cannot revive a non-retryable failure.
- `m2:evaluate` against the completed sequence writes ONLY to the derived directory; the sealed root and its state stay byte-identical.
- The packaging-recovery drill (focused re-audit finding 3) completes a real deterministic attempt, fails the FIRST packaging transaction by injection at the receipt stage — leaving an inventoried final root, a committed-but-unreceipted archive 001, and a `packaging`-stage failure state — and then asserts that `--resume` takes the packaging-ONLY path: `packagingRecovery` reported, the process log and `vite.log` byte-identical (nothing launched), the root still byte-equal to its inventory, archive 001 preserved and recorded as `unreceipted-before-recovery` in `supersededArchives`, archive 002 written with sidecar and receipt, and a subsequent resume a verified no-op.
- **Artifact contract, precisely:** a SUCCESSFUL attempt always saves `attempt-trace.zip`, `final-screenshot.png`, `final-dom.html`, `console-log.json`, and `diagnostics-manifest.json`. A BROWSER-stage failure saves the failure-named equivalents instead — `failure-trace.zip`, `failure-screenshot.png`, `failure-dom.html`, `failure.json`, `console-log.json`, `diagnostics-manifest.json` — plus `failure-message.txt`, `failure-manifest.json`, and `context-logs.txt`. A POST-browser (finalization-stage) failure keeps the success-named browser forensics AND adds the failure records, because the browser phase already completed.
- An incidental validation during development: the resume-identity check refused a real mid-sequence configuration change (the package version was bumped while a rehearsal sequence was in flight, and `--resume` was refused with `resume-identity-mismatch: packageVersion: state 1.7.0 != current 1.8.0`) — the §19.11 mechanism catching exactly what it exists to catch.

## 4. Interpretation decisions

1. **`m2:resume` is `m2:orchestrate -- --resume`** (§19.2 allows this when documented — here, in the CLI header, and in the README).
2. **`m2:pilot` is a refusal stub**: the pilot is brief §31 Phase 7 (rehearsal + pilot, after the Phase 6 adversarial audit) and requires the Phase 4 condition and Phase 5 policy system; the command exists so the §19.2 surface is stable, and it exits 1 with an explanation. `formal-v1.json` and `pilot.json` plans are deliberately NOT shipped — a committed plan naming conditions that do not exist yet would be an executable lie.
3. **Deterministic attempts produce ledger-only evidence** (validated + bare fingerprint): the run-directory pipeline is model-condition-only by frozen design (no gateway seed manifest, no bundle export under the baseline condition). This matches the M1 evidence model exactly.
4. **The sequence identity records the Milestone 1 experiment** (`model-backed-npc-001` v1.2.0) because Phase 3 drives exactly those paths; the M2 experiment identity joins the plan schema in Phase 4.
5. **Rehearsal pacing is 20×** under `evidentiary: false` (§19.17 allows accelerated fake-adapter rehearsals); the schema refuses any non-1× pacing the moment a plan declares itself evidentiary.
6. **The planned gateway stop is tick-triggered** (the M1 Run 6 drill). A mid-flight stop is legal evidence — the finalizer's disposition taxonomy classifies post-stop requests — and the rehearsal asserts the enriched fingerprint preserves emitted > attempted.
7. **Keyless env pinning**: fake-gateway children receive every numeric/config setting explicitly, because the gateway's loader falls back to `.env.gateway` FILE values for unset variables and a rehearsal must never depend on a developer's local file. The orchestrator itself never reads that file in any mode.
8. **Ports are constants** (5199/8799) rather than plan fields: §19.6 wants fixed automation ports with refusal, not configuration.

## 5. Test evidence

- Full suite at the final-targeted-audit-remediated head: **77 files, 772 tests passing** (locally; the exact-head CI run is authoritative). The final round adds `inventory-validation.test.ts` (the strict shared validator: the aggregate-recompute regression, the 14-case malformed matrix, read-time authentication) and extends `process-terminal-state.test.ts` (nonterminal post-spawn errors with visible diagnostics, failed-kill-then-real-exit, classification only after genuine termination, unkillable-child `stop-unconfirmed`) and `packaging-recovery.test.ts` (the §4.4 aggregate-preserving entry-rewrite refusal before any mutation, the §5.4 competing-invocation no-op race drill, and the observed-mutation-while-waiting refusal). The focused round adds three suites and extends three more: `process-terminal-state.test.ts` (real spawned children across every terminal shape — exit 0, nonzero, SIGTERM, SIGKILL, spawn error — signal-killed children never reported running, and the four-way gateway terminal-state classification); `packaging-recovery.test.ts` (failure injection at all seven post-inventory packaging points, each recovered through the REAL `orchestrateSequence` resume with nothing launched and no inventoried byte changed, plus stale-staged cleanup, tamper/divergence refusals, and the full orchestrator-recorded failure loop); `manifest-reconciliation.test.ts` (the §7.2 divergence matrix: removed/added/reordered executions and every edited field, the checkpoint prefix rule, attempt coverage, all-mismatches-listed); extended `freeze-pipeline.test.ts` (study freeze-record per-field tampering, byte-preserving reserialization, unparseable and deleted records), `child-boundary.test.ts` (Chromium enumerated: headless/headed spawned observation, display-allowlist-only headed restoration, launch source binding), and `evidence-lifecycle.test.ts` (control-state edits refused by manifest reconciliation on the completed path). The m2 suites cover: plan-schema cross-rules, the §19.14 budget refusal, evidentiary formal-timing enforcement, the attempt-profile binding with the closed retryable-class enum and the evidentiary/live `formal-attempt-profile-required` refusal; the closed failure taxonomy with disposition assessment; threshold evaluation incl. the under-covered-run `invalid-treatment` classification; the full §6.5 study-reconciliation mismatch matrix; atomic control-root sequence state with 16-field resume-identity refusal, terminal seals for completed AND failed executions, interrupted-execution sealing, and disposition-gated resume; every `checkFreeze` drift kind (HEAD, dirty worktree, package + lockfile, archived plan + config fingerprint, attempt profile, archived study + freeze record + changed external study file) plus the post-sequence checkpoint control-flow matrix (drift at each of pre/post-batch, pre/post-evaluation, pre/post-package fails preserved, never packages, never receipts); the completed-sequence lifecycle over a REAL deterministic fixture (intact verification; archive/sidecar/receipt deletion + tampering refusals; receipt field disagreement; post-completion root mutation; seal-valid-but-semantically-stale refusals; derived-output immutability; versioned repackaging); typed finalization-stage injection drills with forensics preservation; process-boundary canaries proven by SPAWNED children plus keep-awake ordering; release-metadata agreement; the fake gateway's structural `.env.gateway` refusal; portable-packaging invariants (immutable destinations, same-volume staging, no partial archives, no receipt without commit); secret-scan patterns; and the REAL keyless fake-gateway child cycle. `npm run test:m2` scopes the m2 suites for §19.2.
- e2e `automation-contract.spec.ts` pins every contract selector to the live DOM (e2e 10/10).
- `npm run validate` 0/0; batch PASSED with all fourteen goldens byte-identical; gateway suite green; model-bundle suite green; builds + dist secret scan; keyless model rehearsal ×3 strict-completed; `audit:affordances` OK (4 known / 0 unregistered / 4-4 coverage).
- CI runs `test:m2`, the keyless `m2:rehearse -- --ci` (now including the packaging-recovery drill), and the hard-path evidence upload (versioned archive + receipt + manifest + control-root state + failure drill + the packaging-drill's superseded archive 001 beside its recovered receipted archive 002) after the audit step per A10; job timeout 45 minutes.

## 6. Known limitations and scheduled work

1. The unattended acceptance test proper (§19.16 Stage A) belongs to Phase 4: it requires the M2 per-decision condition and one live run. Phase 3 proves the machinery keylessly, including the gateway-stop path demonstrated keylessly before any live spend, exactly as §19.16 requires. Until Phase 4 registers a FORMAL attempt profile with its study, every evidentiary or live plan is hard-refused at parse time (`formal-attempt-profile-required`).
2. `m2:pilot` refuses until Phase 7 (see decision 2).
3. The keep-awake lease is best-effort and live-only; the §19.13 interlock (`--allow-sleep-risk`) is enforced for live plans only, and no live plan exists yet.
4. Cross-run evaluation currently reports Mara composites for comparable pairs; the full R2/R3 statistics arrive with those registered studies.
5. **Scheduled before live Stage A (re-audit §13.4; focused re-audit §8.2):** genuinely bounded or chunked/rotating Playwright tracing for multi-hour 1× attempts, evidence-size forecasting, and explicit retention behavior. Phase 3 already bounds tracing (no source embedding), preflights free disk against the per-plan evidence budget, measures the root before and after every execution, caps archive size, and time-bounds nested scans — but formal 1× evidence sizes must be MEASURED during Stage A, not extrapolated, before the 25-run sequence is authorized.
6. **Scheduled with Phase 4's formal study registration (focused re-audit §8.1):** exact observation-cell and replicate-multiplicity binding (or direct plan-hash binding) in study reconciliation for the later multi-condition formal design. The current condition/scenario/seed-set + total-n comparison is sufficient for the initial single-cell R2 variance study, and every formal or live plan stays hard-refused (`formal-attempt-profile-required`) until that registration lands.
